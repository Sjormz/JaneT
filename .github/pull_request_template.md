## Summary

What does this PR change?

## Why

Why is this change needed?

## Testing

- [ ] `node --version` matches CI's Node major (record OS/runtime below)
- [ ] `npm run typecheck`
- [ ] `npm test`
- [ ] `npm run build`
- [ ] Available-OS desktop smoke from `ci.yml`, plus affected E2E specs
- [ ] Full E2E for shared app/terminal/persistence/lifecycle changes

Commands, pass/fail/skip counts, and untested platforms (or docs-only reason):

## Release impact

Patch / minor / breaking / no app release, with a brief reason:

## Screenshots / videos

Include screenshots or a short screen recording for UI changes.

## Notes

Anything else reviewers should know?
